# Let Nixpacks auto-detect Python
providers = ["python"]

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.build]
cmds = [
    "cd web && npm install && npm run build"
]

[start]
cmd = "gunicorn api.server:app --bind 0.0.0.0:$PORT"
